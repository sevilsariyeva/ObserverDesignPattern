<UserControl x:Class="Youtube_Observer_.Views.UserControls.HomeUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Youtube_Observer_.Views.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.Background>
            <LinearGradientBrush>
                <GradientStop Color="DarkSlateGray" Offset="0"></GradientStop>
                <GradientStop Color="Black" Offset="1"></GradientStop>
            </LinearGradientBrush>
        </Grid.Background>
        <Label Width="350" Height="150" Margin="0,0,0,150">
            <Image Stretch="Fill" Source="/Icons/youtube.png"></Image>
        </Label>
        <Button Style="{StaticResource mainButtons}" Command="{Binding YoutuberClickCommand}" Width="250" Height="80" Margin="147,326,453,74" Content="Youtuber"></Button>
        <Button Style="{StaticResource mainButtons}" Command="{Binding SubscriberClickCommand}" Width="250" Height="80" Margin="475,326,125,74" Content="Subscriber"></Button>
    </Grid>
</UserControl>
